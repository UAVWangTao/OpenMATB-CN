# OpenMATB 实验操作说明（被试/玩家版）

## 一、整体流程

1. **启动程序** → 出现「选择场景」界面  
2. **选一个场景**（上下键移动，回车选中）→ 若场景里包含问卷，会先做问卷（按空格继续/确认）  
3. **进入主任务界面** → 屏幕同时显示 4 个子任务 + 右上角调度时间轴  
4. **尽量同时做好 4 件事**，直到场景结束（或你按 **P** 暂停 / **Esc** 退出）

---

## 二、主界面里你要做的 4 件事

### 1. 系统监控（左上，六块仪表）

- **看什么**：6 个仪表，每个有**灯**（绿/红）和**指针/刻度**。
- **你要做的**：
  - 绿灯灭了 → 按对应键把它“纠正”回来；
  - 红灯亮了 → 按对应键关掉；
  - 某个刻度上的**箭头跑出正常范围**（偏到一端）→ 按对应键把箭头调回中间。
- **默认按键**：仪表 1～4 一般是 **F1、F2、F3、F4**，两个灯一般是 **F5、F6**（具体以场景或说明为准；有的场景会改成例如 F8 等）。
- **要点**：发现异常就按对对应的键，反应要快一点。

---

### 2. 跟踪任务（中间，一个圆圈 + 十字准星）

- **看什么**：一个**圆圈**（目标区）和一个会**自己乱飘的十字准星**。
- **你要做的**：用**操纵杆（摇杆）**把十字准星**尽量保持在圆圈里**。
- **操作**：摇杆往哪推，准星就往哪动；持续微调，别让准星跑出圈太久。
- **没有摇杆**：程序可能报错或无法正常做这项任务，这是 MATB 设计需求。

---

### 3. 通讯任务（右上，无线电）

- **看什么**：若干**无线电**，每个有频率和呼号；耳机里会**念指令**（例如让你调某个频率、或呼叫你的呼号）。
- **你要做的**：
  - 只响应**呼叫你呼号**的指令（例如 “TIX580 请调频到 …”），**别人的呼号不用管**；
  - 用**上下键**选中要操作的那部电台；
  - 用**左右键**调频率到指令里说的数字；
  - 调好后按**回车**确认。
- **默认按键**：上/下选电台，左/右调频率，**回车**确认。
- **要点**：听清是不是在叫你（你的呼号会显示在界面上），别被别的呼号干扰。

---

### 4. 资源管理（下方，油箱 + 油泵）

- **看什么**：两个**主油箱 A、B**（要维持在绿色目标区），多个**辅助油箱**和**油泵**（标了 1～8）。
- **你要做的**：开/关油泵，让 A、B 的液面尽量**保持在绿色目标带**里（不要太高或太低）。
- **默认按键**：小键盘 **1～8** 对应油泵 1～8，按一下开，再按一下关。
- **注意**：有的油泵会**故障**（变红或不可用），就不能再靠它了，用别的泵补；同时要兼顾 A 和 B 两个罐子。

---

## 三、右上角「调度」是干啥的

- 那里是**时间轴**，显示各任务在**什么时候开始、什么时候结束**，以及是否被**自动化接管**（例如显示 AUTO）。
- 你不用去“操作”它，只是**看一眼**就知道当前该关注哪几个任务、哪些已经自动做了，方便分配注意力。

---

## 四、常用快捷键（全局）

| 按键 | 作用 |
|------|------|
| **P**（或 **p**） | 暂停实验；出现「暂停」弹窗后按**空格**继续 |
| **Esc** | 弹出「是否退出」；选退出则结束实验 |

**说明**：若按 **P** 没反应，请确保当前没有其他弹窗（如会话 ID、问卷）；有弹窗时先按空格关掉再按 P。程序已兼容 P / p 两种键名。

---

## 五、小结：进去之后“该干什么”

- **同时**做四件事：  
  **看仪表按 F1～F6** + **摇杆稳住准星** + **听电台调频率并回车** + **小键盘 1～8 调油泵**。
- 右上角时间轴帮你判断**当前哪些任务在进行**。
- 尽量**少漏**仪表异常、**少让**准星跑出圈、**准时**响应自己的呼号、**把 A/B 液面**维持在绿带里，这样表现会更好。

如果你是用 **basic** 或 **default** 场景，一开始可能先做一页 NASA-TLX 问卷（按空格继续/确认），然后就会进入上面这个主界面。

**为什么玩一会儿就自动退了？**  
这是正常现象：场景里规定了每个任务**开始和结束的时间**，到了结束时间（例如 basic 场景约 1 分钟）所有任务会一起停止，程序就会自动退出并保存日志。想玩更久可以选更长的场景，或在「场景生成器」里自己编一个更长时长的场景。
